# @idp-v2/common-constructs

This library was generated with [@aws/nx-plugin](https://github.com/awslabs/nx-plugin-for-aws/).

## Building

Run `pnpm exec nx build @idp-v2/common-constructs [--skip-nx-cache]` to build the application.

## Running unit tests

Run `pnpm exec nx test @idp-v2/common-constructs` to execute the unit tests via Vitest.

### Updating snapshots

To update snapshots, run the following command:

`pnpm exec nx test @idp-v2/common-constructs --configuration=update-snapshot`

## Run lint

Run `pnpm exec nx lint @idp-v2/common-constructs`

### Fixable issues

You can also automatiaclly fix some lint errors by running the following command:

`pnpm exec nx lint @idp-v2/common-constructs --configuration=fix`

## Useful links

- [common-constructs reference docs](TODO)
- [Learn more about NX](https://nx.dev/getting-started/intro)
