[
  {
    "name": "load_artifact",
    "description": "Load an existing artifact from the project. Use this tool when you need to read or reference a previously saved artifact.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "artifact_id": {
          "type": "string",
          "description": "The unique identifier of the artifact to load"
        }
      },
      "required": ["artifact_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "artifact_id": {
          "type": "string",
          "description": "Unique identifier of the artifact"
        },
        "filename": {
          "type": "string",
          "description": "Original filename"
        },
        "content_type": {
          "type": "string",
          "description": "MIME type of the artifact"
        },
        "content": {
          "type": "string",
          "description": "The content of the artifact (base64 for binary, plain text for text)"
        },
        "encoding": {
          "type": "string",
          "description": "Content encoding ('base64' or 'text')"
        },
        "created_at": {
          "type": "string",
          "description": "ISO 8601 timestamp of when the artifact was created"
        }
      }
    }
  }
]
